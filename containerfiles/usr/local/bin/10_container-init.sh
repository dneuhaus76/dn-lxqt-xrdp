#!/bin/bash
#10_container-init.sh - Dieses Script wird zur Container-Laufzeit benutzt z.B. für aktionen in tmpfs - die während BUILD-install nicht existieren
# besonders ist auf Gruppen ids innerhalb des Containers zu achten


# X11 Sockets Verzeichnis - evtl. als workaround
#mkdir -pv /tmp/.X11-unix /run/user
#chmod -v 1777 /tmp/.X11-unix /run/user


exit 0
